# npm workspace configuration
# Ensures package-lock.json is always created and updated
package-lock=true

# Save exact versions (recommended for workspaces to avoid version conflicts)
save-exact=false

# Automatically install peer dependencies
legacy-peer-deps=false

# Don't run scripts automatically (security best practice)
ignore-scripts=false

